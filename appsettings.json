{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "EmployeeDbConnection": "Data Source=EmployeeDb.sqlite"
  }
}


//server=localhost;port=3306;database=employeedata;user=root;password=benspassword
//Since we are not deploying this, I figured it would be easier to just have the code build the database
// with dummy data for simplicity. But if we eventually move to a db on someone's computer we can use this^^^ -Ben